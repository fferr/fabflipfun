{
  "name": "fabflipfun",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "android": "lerna --scope=mobile run android",
    "bootstrap": "lerna clean --yes && lerna bootstrap --use-workspaces",
    "format": "lerna run format --ignore mobile",
    "ios": "lerna --scope=mobile run ios"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "mobile/**"
    ]
  },
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
